export enum Variants {
  PRIMARY = 'primary',
  SECONDARY = 'secondary',
  ACCENT = 'accent',
  GHOST = 'ghost',
}

export type Variant = `${Variants}`;

export enum Sizes {
  DBL_XS = '2xs',
  XS = 'xs',
  SM = 'sm',
  MD = 'md',
  LG = 'lg',
  XL = 'xl',
  DBL_XL = '2xl',
}

export type Size = `${Sizes}`;
